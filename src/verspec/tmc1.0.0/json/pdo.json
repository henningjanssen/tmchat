{
  "/login":{
    "stmt":"SELECT * FROM `Users` WHERE `mail` LIKE :user",
    "values":{
      "user":{
        "pdokey":":user", "msgkey":"user", "dbkey":"user", "type":"string"
      },
      "password":{
        "pdokey":":password", "msgkey":"password",
        "dbkey":"password", "type":"string"
      }
    }
  }
}
